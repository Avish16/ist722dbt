version: 2

models:
  - name: fact_sales
    description: "Fact table storing product-level sales transactions."

    columns:
      - name: orderid
        description: "Identifier for the order associated with this line item."
        tests: [not_null]

      - name: productid
        description: "Identifier for the product in the order."
        tests: [not_null]
