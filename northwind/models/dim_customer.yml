version: 2

models:
  - name: dim_customer
    description: "Dimension table containing customer profile and contact information."

    columns:
      - name: customerid
        description: "Natural key representing a unique customer."
        tests: [unique, not_null]

      - name: companyname
        description: "Customer's company name."
